# Member portraits

Upload each member's portrait here with a short filename such as `jerry-zou.jpg`.
Add their actual name, role, short bio and unique ID to `content/members.json`.
Set `avatar` to `/members/jerry-zou.jpg`, or leave it null until supplied.
The Members section is generated automatically. Use the same member ID in a
field note's `author` field to link their name to their profile.
