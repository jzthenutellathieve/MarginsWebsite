{
  "name": "the-margins-newsletter",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build.cjs",
    "test": "node --test test/*.test.cjs"
  },
  "engines": { "node": "22.x" }
}
